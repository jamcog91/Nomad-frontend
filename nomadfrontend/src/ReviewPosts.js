// import { createBrowserRouter, browserRouter } from 'react-router-browser'
// import { useNavigate } from "react-router-dom";
function ReviewPosts({reviews, setReviews}) {

const ReviewPost = ({reviews, setReviews}) => {

    // const navigate = useNavigate()
    // const openProfile = () => {
    //     navigate('/profilePage/${user.id}')
        
    // }
}

return(

    <div className="review-card">
        {/* <img src={user.avatar} />
        <h1>{user.handle}</h1>
        <p>{user.name} ranked {restaurant.name}</p>
        <h2>{user.review.score}</h2>
        <p>{user.review.likes}</p> */}
        <button>like</button>
        <button>comment</button>
        <button>add review</button>
    </div>
    )
}
export default ReviewPosts;